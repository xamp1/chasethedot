{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "Classic snake game built in vanilla JavaScript using the Canvas API",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
  
}
